services:
  llamafactory:
    build:
      dockerfile: ./docker/docker-cuda/Dockerfile
      context: ../..
      args:
        INSTALL_BNB: true
        INSTALL_VLLM: true
        INSTALL_DEEPSPEED: true
        INSTALL_FLASHATTN: true
        INSTALL_LIGER_KERNEL: false
        INSTALL_HQQ: true
        INSTALL_EETQ: true
        PIP_INDEX: https://pypi.org/simple
    container_name: llamafactory-vortez
    volumes:
      - ../../hf_cache:/root/.cache/huggingface
      - ../../ms_cache:/root/.cache/modelscope
      - ../../om_cache:/root/.cache/openmind
      - ../../data:/app/data
      - ../../output:/app/output
    ports:
      - "7886:7860"
      - "8600:8000"
    ipc: host
    tty: true
    shm_size: '16gb'
    stdin_open: true
    command: ./start.sh
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            count: "all"
            capabilities: [gpu]
    restart: unless-stopped
